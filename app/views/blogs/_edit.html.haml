.blogform
  = form_for @blog do |f|
    .blogform__title
    = f.label :title
    %br/
    = f.text_area :title do
      - text blog.title if blog.title
    = f.label :text
    %br/
    = f.text_area :body, class:"blogform__text" do
      - text blog.body if blog.body
    = f.submit 'Send', class: 'form__submit' 